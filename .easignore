# .easignore
# Exclude everything standard
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Exclude native folders (Let EAS Prebuild generate clean ones)
android/
ios/
.kotlin/

# Exclude large documentation/report files
*.pdf
*.md

# Exclude debug/error logs
npm-debug.*
yarn-debug.*
yarn-error.*

# Exclude secret keys that are NOT needed for the build
*-firebase-adminsdk-*.json

# !!! EXCEPTIONS (Files we WANT to upload) !!!
# We want these, so we DO NOT list them here (or we verify they aren't matched above)

# google-services.json is NOT ignored here, so it WILL be uploaded.
# .env is NOT ignored here, so it WILL be uploaded.
