# .easignore - Files to exclude from EAS Build uploads
# Inherit from .gitignore
# (Files in .gitignore are automatically excluded unless you create .easignore, 
#  but creating .easignore overrides .gitignore, so we should copy relevant parts or include everything)

# Ignore the backend folder completely
backend/

# Ignore local builds/artifacts
android/
ios/
.expo/
dist/
web-build/

# Ignore node_modules (EAS installs them fresh)
node_modules/

# Ignore sensitive env files (EAS uses Secrets)
.env
.env.local
.env.*

# Ignore logs and temp files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
